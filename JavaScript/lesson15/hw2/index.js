const createLogger = () => {

    const warnText = '';
    const errorText = '';
    const logText = '';

    const warn = text => {
        warnText = text;
    }

    const error = text => {
        errorText = text;
    }

    const log = text => {
        logText = text;
    }

    const getRecords = () => {
        // if ()
    }
}